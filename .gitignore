# ── Secrets ──────────────────────────────────────────────────────────────────
.env

# ── Build cache (ruwe scrape-output, niet nodig in repo) ─────────────────────
cache/

# ── Node ─────────────────────────────────────────────────────────────────────
node_modules/

# ── Gegenereerde web-assets (worden gebouwd door pipeline) ───────────────────
# Bewust NIET genegeerd: we committen de kant-en-klare web/ folder
# zodat GitHub Pages direct kan deployen zonder re-build.
# Verwijder onderstaande regels als je de assets wil negeren.
# web/data.json
# web/assets/

# ── Claude Code sessiedata ───────────────────────────────────────────────────
.claude/

# ── OS / IDE ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
*.log
